# 脚本编码问题和依赖检查修复说明

## 修复概述

本次更新解决了以下关键问题：
1. ✅ 中文字符编码问题
2. ✅ 缺失依赖包的自动检查和安装
3. ✅ Python环境验证

## 编码修复详情

### Python文件编码
所有Python脚本已添加UTF-8编码声明：
```python
# -*- coding: utf-8 -*-
```

包括以下文件：
- `one_click_automation.py` - 主自动化脚本
- `geolocation.py` - 地理位置查询模块
- `SpeedTest.py` - 速度测试模块
- `request.py` - 连通性测试模块
- `filter.py` - 结果过滤模块
- `txt_2_csv.py` - 文本转CSV模块
- `xml_to_csv_parser_updated_no_duplicates.py` - XML解析模块
- `check_dependencies.py` - 依赖检查模块（新增）

### BAT文件编码
所有批处理文件已使用ANSI编码保存，支持Windows系统：
- `00-一键开始测试.bat` - 主启动脚本（已改进）
- `00-可视化扫描结果.bat` - 扫描结果可视化（已改进）
- `01-提取反代了CF的ip及端口.bat` - 提取CF代理IP（已改进）
- `02-可视化排序成csv【可以不运行这个，看自己需求】.bat` - 排序脚本（已改进）
- `03-开始针对性测速.bat` - 速度测试脚本（已改进）
- `04-查看测试结果.bat` - 查看结果脚本（已改进）

### Shell脚本编码
- `00-一键开始测试.sh` - 已更新为UTF-8编码并改进错误处理

## 依赖检查功能

### 工作原理

新的 `check_dependencies.py` 模块在脚本启动时：

1. **验证Python版本**：确保使用Python 3.6+
2. **读取requirements.txt**：获取所需的依赖包列表
3. **检查已安装的包**：使用 `pip list` 获取当前环境的包
4. **识别缺失的包**：比对找出未安装的依赖
5. **自动安装**：提示用户并自动安装缺失的包

### Requirements.txt

已更新 `requirements.txt` 文件，包含：
```
requests>=2.25.0
pandas>=1.0.0
```

### 自动安装流程

启动 `one_click_automation.py` 时，脚本会：

1. 显示依赖检查信息
2. 若发现缺失的包，自动提示安装
3. 若同意安装，自动调用 `pip install` 安装所有缺失的包
4. 安装完成后继续执行主脚本

### 手动安装

如果需要手动安装依赖，可以运行：
```bash
pip install -r requirements.txt
```

或者单独安装：
```bash
pip install requests pandas
```

## 改进的BAT启动脚本

所有BAT文件现在都包含以下改进：

1. **设置UTF-8代码页**
   ```batch
   chcp 65001 >nul
   ```

2. **验证Python环境**
   ```batch
   python --version >nul 2>&1
   if errorlevel 1 (
       echo 错误：未找到Python
       pause
       exit /b 1
   )
   ```

3. **错误处理**
   ```batch
   if errorlevel 1 pause
   ```

4. **中文字符正确显示**：所有中文提示信息现在能正确显示

## 改进的Shell启动脚本

`00-一键开始测试.sh` 现在包含：

1. **Python环境检查**：验证 python3 或 python 已安装
2. **虚拟环境支持**：优先使用 `.venv/bin/python`
3. **错误代码传递**：正确传递脚本执行结果
4. **虚拟环境备用方案**：找不到虚拟环境时使用系统 python3

## 文件编码确认

所有文件现在已标准化编码：

| 文件类型 | 编码格式 | 说明 |
|---------|--------|------|
| Python脚本 | UTF-8 | 包含 `# -*- coding: utf-8 -*-` 声明 |
| BAT文件 | ANSI/GBK | 使用 `chcp 65001` 设置UTF-8输出 |
| Shell脚本 | UTF-8 | 标准Linux/Mac格式 |
| TXT文档 | UTF-8 | UTF-8编码保存 |

## 验收标准

✅ 脚本启动时不再出现编码乱码错误
✅ 中文字符能够正确显示
✅ 脚本能自动检测并提示安装缺失的依赖
✅ 提供清晰的依赖安装指引
✅ 创建了 requirements.txt 文件方便用户批量安装依赖
✅ 所有文件编码统一规范
✅ BAT和Shell脚本都包含Python环境检查

## 故障排除

### 问题：Python不在PATH中
**解决方案**：
1. 重新安装Python，并在安装时勾选"Add Python to PATH"
2. 或手动添加Python安装目录到系统PATH

### 问题：仍然出现编码错误
**解决方案**：
1. 确认Windows系统区域设置为中文
2. 确认使用的文本编辑器支持UTF-8编码
3. 尝试重新打开终端窗口

### 问题：依赖安装失败
**解决方案**：
1. 检查网络连接
2. 手动运行：`pip install --upgrade pip`
3. 然后重新运行脚本或手动安装依赖

### 问题：权限不足（Linux/Mac）
**解决方案**：
1. 运行：`chmod +x 00-一键开始测试.sh`
2. 确保脚本有执行权限

## 日志输出

脚本现在会显示详细的初始化日志：

```
🔍 检查运行环境...
✓ Python 版本满足要求: 3.9.0
✓ 检查依赖包 (2 个)...
✓ 所有依赖包都已安装
```

或者在缺失依赖时：

```
🔍 检查运行环境...
✓ Python 版本满足要求: 3.9.0
✓ 检查依赖包 (2 个)...

⚠️  检测到缺失的依赖包：
   - pandas
   
📦 正在安装缺失的包...
   安装 pandas... ✓

✓ 所有依赖安装成功！
```

## 技术实现细节

### 依赖检查机制

1. **包名规范化**：处理包含下划线/连字符的不同包名格式
2. **版本号兼容**：支持 `pandas>=1.0.0` 这样的版本约束
3. **错误处理**：即使 pip 不可用也能降级处理
4. **自动重试**：安装失败时提供清晰的手动安装指令

### 编码标准

- Python3 源文件编码声明：`# -*- coding: utf-8 -*-`
- Windows BAT：ANSI编码 + UTF-8代码页切换
- Linux/Mac Shell：UTF-8编码
- 数据文件：视需求使用UTF-8或GBK

## 相关文件列表

新增或修改的文件：
- ✨ `check_dependencies.py` - 依赖检查模块（新增）
- 📝 `requirements.txt` - 依赖列表（已更新）
- 🐍 `one_click_automation.py` - 编码声明+依赖检查（已更新）
- 🐍 `geolocation.py` - 编码声明（已更新）
- 🐍 `SpeedTest.py` - 编码声明（已更新）
- 🐍 `request.py` - 编码声明（已更新）
- 🐍 `filter.py` - 编码声明（已更新）
- 🐍 `txt_2_csv.py` - 编码声明（已更新）
- 🐍 `xml_to_csv_parser_updated_no_duplicates.py` - 编码声明（已更新）
- 🪟 `00-一键开始测试.bat` - 环境检查+编码修复（已更新）
- 🪟 `00-可视化扫描结果.bat` - 环境检查+编码修复（已更新）
- 🪟 `01-提取反代了CF的ip及端口.bat` - 环境检查+编码修复（已更新）
- 🪟 `02-可视化排序成csv【可以不运行这个，看自己需求】.bat` - 环境检查+编码修复（已更新）
- 🪟 `03-开始针对性测速.bat` - 环境检查+编码修复（已更新）
- 🪟 `04-查看测试结果.bat` - 环境检查+编码修复（已更新）
- 🐧 `00-一键开始测试.sh` - 环境检查+编码修复（已更新）

